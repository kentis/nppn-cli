StringBuffer sb = new StringBuffer();
for(int i = 0; i < ((List)${params[0]}).size(); i++){
	sb.append( ((List) ((List)${params[0]}).get(i)).get(1) );
	
}
${params[1]} = sb.toString();
%%VARS:${params[0]}, ${params[1]}%% 